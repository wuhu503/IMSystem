# IMSystem — Qt6 即时通讯系统

> C/S 架构即时通讯系统，基于 Qt 6.11 + C++17 + SQLite + CMake 构建。

---

## 项目结构

```
IMSystem/
├── common/                    # 公共模块（客户端/服务端共享）
│   ├── protocol.h             # 消息协议定义
│   ├── message.h/.cpp         # 消息序列化/反序列化
│   └── utils.h/.cpp           # 工具函数（加密、时间、UUID）
│
├── server/                    # 服务端
│   ├── main.cpp               # 服务端入口
│   ├── tcpserver.h/.cpp       # TCP 服务器
│   ├── clienthandler.h/.cpp   # 客户端连接处理
│   ├── database/
│   │   ├── init.sql           # 建表脚本
│   │   └── dbmanager.h/.cpp   # 数据库管理（单例）
│   └── service/
│       └── authservice.h/.cpp # 认证服务
│
├── client/                    # 客户端
│   ├── tcpclient.h/.cpp       # TCP 客户端
│   └── ui/
│       ├── logindialog.*      # 登录界面
│       └── registerdialog.*   # 注册界面
│
├── main.cpp                   # 客户端入口
├── mainwindow.h/.cpp/.ui      # 主窗口（待开发）
└── CMakeLists.txt             # 构建配置
```

---

## 已完成

### 公共模块

- **消息协议**：16 字节固定头 + JSON 消息体，支持多种消息类型（注册、登录、好友、聊天等）
- **消息封装**：序列化/反序列化，通过 QTcpSocket 可靠传输
- **工具函数**：SHA256 + 盐值密码加密、UUID 生成、时间工具

### 服务端

- **TCP 服务器**：基于 QTcpServer，支持多客户端并发连接
- **客户端连接处理**：粘包处理、消息解析与分发
- **数据库层**：SQLite + 单例模式，含 users / friendships / messages 三张表
- **认证服务**：注册（参数校验、用户名去重、密码加密存储）、登录（密码验证、Token 生成、在线状态更新）

### 客户端

- **TCP 客户端**：单例模式，连接管理、消息收发
- **登录界面**：服务器地址/端口输入、账号密码登录、跳转注册
- **注册界面**：用户名/密码注册、参数校验、返回登录

### 基础设施

- CMake 构建：公共模块静态库 + 服务端/客户端双目标
- Git 版本管理，已推送至 GitHub

---

## 待完成

### 优先级高

| 功能 | 说明 |
|------|------|
| **主窗口 UI** | MainWindow 目前为空壳，需要实现聊天主界面 |
| **好友系统** | 搜索用户、添加/删除好友、好友列表、在线状态 |
| **一对一聊天** | 文本消息收发、消息存储、历史消息查询 |

### 优先级中

| 功能 | 说明 |
|------|------|
| **群聊系统** | 创建群组、加入群组、群消息收发 |
| **消息可靠性** | 消息确认（ACK）、离线消息推送、消息去重 |
| **心跳机制** | 客户端定期心跳、服务端超时断开 |

### 优先级低

| 功能 | 说明 |
|------|------|
| **文件/图片传输** | 文件分片上传、图片消息 |
| **用户头像/昵称** | 个人信息编辑、头像上传 |
| **消息已读状态** | 已读回执、未读计数 |
| **断线重连** | 网络异常检测、自动重连 |
| **日志系统** | 文件日志、日志级别控制 |
| **多线程优化** | 服务端连接处理移至线程池 |

---

## 技术要点

| 项目 | 方案 |
|------|------|
| 消息格式 | 16 字节固定头 + JSON Body |
| 数据库 | SQLite，单例模式访问 |
| 密码安全 | SHA256 + 随机盐值，不明文存储 |
| 粘包处理 | 缓冲区 + 长度前缀 |
| 认证 | Token（UUID），内存存储 |
| 构建 | CMake，common 静态库 + 双可执行目标 |

---

## 运行方式

```bash
# 构建
cmake -B build -G "MinGW Makefiles"
cmake --build build

# 先启动服务端
./build/IMServer.exe

# 再启动客户端
./build/IMClient.exe
```